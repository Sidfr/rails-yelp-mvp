<h1>Restaurants#index</h1>
<p>Find me in app/views/restaurants/index.html.erb</p>

<h1>LISTE DES RESTAURANTS</h1>
<hr>
<% @restaurants.each do |restaurant| %>
<%= "#{restaurant.id}: #{restaurant.name} - #{restaurant.address} // #{restaurant.phone_number} - #{restaurant.category}" %> </h2>
<p>
<%= link_to "Edit", edit_restaurant_path(restaurant), method: :get%>
<%= link_to "Show", restaurant_path(restaurant), method: :get%>
<%= link_to "Add a review", new_restaurant_review_path(restaurant), method: :get%></p>
</p>
<hr>
<% end %>

